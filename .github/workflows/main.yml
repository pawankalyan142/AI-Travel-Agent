name: AI Code Review (Final)

on:
  pull_request:
    types: [opened, synchronize, reopened]

env:
  MAX_TOTAL_LINES: 1000   # PR-level hard limit
  MAX_LINES_PER_FILE: 300  # Safety split threshold per file
  CONTEXT_LINES: 10        # Increased — more context per file is now valuable

permissions:
  pull-requests: write
  contents: read

jobs:
  ai-code-review:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout full repo
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Step 2: Check if AI already reviewed this PR
      - name: Check if AI already reviewed this PR
        id: check_reviewed
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          python3 << 'EOF'
          import urllib.request, json, os
          token = os.environ["GITHUB_TOKEN"]
          repo = os.environ["REPO"]
          pr_number = os.environ["PR_NUMBER"]
          req = urllib.request.Request(
              f"https://api.github.com/repos/{repo}/issues/{pr_number}/comments?per_page=100",
              headers={"Authorization": f"Bearer {token}", "Accept": "application/vnd.github.v3+json"}
          )
          with urllib.request.urlopen(req) as r:
              comments = json.loads(r.read())
          already_reviewed = any(
              "AI Code Review Complete" in c.get("body", "") or
              "AI Code Review Skipped" in c.get("body", "")
              for c in comments
          )
          with open(os.environ["GITHUB_OUTPUT"], "a") as f:
              f.write(f"already_reviewed={'true' if already_reviewed else 'false'}\n")
          print(f"Already reviewed: {already_reviewed}")
          EOF

      # Step 3: Stop if already reviewed
      - name: Post already reviewed comment and stop
        if: steps.check_reviewed.outputs.already_reviewed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Content-Type: application/json" \
            "https://api.github.com/repos/$REPO/issues/$PR_NUMBER/comments" \
            -d "{\"body\": \"## \u2139\ufe0f AI Review Already Done\n\n\u2705 AI has already reviewed this PR once. No further AI reviews will run.\n\n**Next step:** Please fix the AI comments and request a **peer review** from your teammates.\"}"

      # Step 4: Get local diff for content + size check
      - name: Get PR diff and validate size
        if: steps.check_reviewed.outputs.already_reviewed == 'false'
        id: validate_diff
        run: |
          git diff origin/${{ github.base_ref }}...HEAD \
            -- '*.py' '*.js' '*.ts' '*.tsx' '*.jsx' \
               '*.java' '*.go' '*.rb' '*.php' '*.cs' \
               '*.cpp' '*.c' '*.h' '*.rs' '*.swift' \
               '*.kt' '*.scala' '*.yaml' '*.yml' '*.json' \
               '*.html' '*.css' '*.scss' \
            > pr_diff.txt
          ADDED_LINES=$(grep -c '^+[^+]' pr_diff.txt || true)
          echo "Added lines in diff: $ADDED_LINES"
          echo "total_lines=$ADDED_LINES" >> $GITHUB_OUTPUT
          if [ "$ADDED_LINES" -gt "$MAX_TOTAL_LINES" ]; then
            echo "too_large=true" >> $GITHUB_OUTPUT
          else
            echo "too_large=false" >> $GITHUB_OUTPUT
          fi

      # Step 5: Hard stop if PR too large
      - name: Comment PR too large and exit
        if: steps.check_reviewed.outputs.already_reviewed == 'false' && steps.validate_diff.outputs.too_large == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Content-Type: application/json" \
            "https://api.github.com/repos/$REPO/issues/$PR_NUMBER/comments" \
            -d "{\"body\": \"## \u26a0\ufe0f AI Code Review Skipped\n\n**Reason:** This PR has **${{ steps.validate_diff.outputs.total_lines }} added lines** which exceeds the limit of 1000 lines.\n\n**Please split this PR into smaller focused PRs.**\"}"
          exit 0

      # Step 6: Build valid lines from GitHub API + FILE-LEVEL chunks
      #
      # CHUNKING STRATEGY: one chunk = one complete file's changes.
      # The AI sees ALL changes in a file together, with full context per line,
      # the filename, language, and total file size. This gives the AI the same
      # view a senior developer has when reviewing a file.
      #
      # Safety split: if a single file has >MAX_LINES_PER_FILE added lines,
      # split it into parts at blank lines to avoid token limit issues.
      #
      # ID system: every added line still gets a numeric ID so the AI never
      # needs to reference file paths — zero hallucination risk.
      - name: Build valid lines from GitHub API and prepare file-level chunks
        if: steps.check_reviewed.outputs.already_reviewed == 'false' && steps.validate_diff.outputs.too_large == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
          CONTEXT_LINES: ${{ env.CONTEXT_LINES }}
          MAX_LINES_PER_FILE: ${{ env.MAX_LINES_PER_FILE }}
        run: |
          python3 << 'EOF'
          import urllib.request, json, re, os

          token = os.environ["GITHUB_TOKEN"]
          repo = os.environ["REPO"]
          pr_number = os.environ["PR_NUMBER"]
          context_size = int(os.environ.get("CONTEXT_LINES", 10))
          max_per_file = int(os.environ.get("MAX_LINES_PER_FILE", 300))

          # ----------------------------------------------------------------
          # PART A: Get valid (file, line) set from GitHub's PR Files API
          # This is the ground truth for what GitHub's comment API accepts.
          # ----------------------------------------------------------------
          valid = set()
          page = 1
          while True:
              req = urllib.request.Request(
                  f"https://api.github.com/repos/{repo}/pulls/{pr_number}/files?per_page=100&page={page}",
                  headers={"Authorization": f"Bearer {token}", "Accept": "application/vnd.github.v3+json"}
              )
              with urllib.request.urlopen(req) as r:
                  files = json.loads(r.read())
              if not files:
                  break
              for f in files:
                  filepath = f["filename"]
                  patch = f.get("patch", "")
                  if not patch:
                      continue
                  current_line = 0
                  for patch_line in patch.split("\n"):
                      if patch_line.startswith("@@"):
                          m = re.search(r'\+(\d+)', patch_line)
                          if m:
                              current_line = int(m.group(1)) - 1
                      elif patch_line.startswith("+"):
                          current_line += 1
                          valid.add((filepath, current_line))
                      elif not patch_line.startswith("-"):
                          current_line += 1
              page += 1

          print(f"Valid lines from GitHub API: {len(valid)}")
          with open("valid_lines.json", "w") as out:
              json.dump([{"file": f, "line": l} for f, l in valid], out)

          # ----------------------------------------------------------------
          # PART B: Parse local diff — group changes BY FILE
          # Each file gets its own list of added lines with full context.
          # This is the core of the file-level chunking strategy.
          # ----------------------------------------------------------------

          # Language detection from extension — tells AI what it's reviewing
          LANG_MAP = {
              "js": "JavaScript", "ts": "TypeScript", "tsx": "TypeScript/React",
              "jsx": "JavaScript/React", "py": "Python", "java": "Java",
              "go": "Go", "rb": "Ruby", "php": "PHP", "cs": "C#",
              "cpp": "C++", "c": "C", "h": "C/C++ Header", "rs": "Rust",
              "swift": "Swift", "kt": "Kotlin", "scala": "Scala",
              "yaml": "YAML", "yml": "YAML", "json": "JSON",
              "html": "HTML", "css": "CSS", "scss": "SCSS"
          }

          def get_language(filepath):
              ext = filepath.rsplit(".", 1)[-1].lower() if "." in filepath else ""
              return LANG_MAP.get(ext, "Unknown")

          # files_data: { filepath: [{"line": N, "content": "..."}, ...] }
          files_data = {}
          current_file = None
          new_line = 0

          with open("pr_diff.txt", "r") as f:
              raw_lines = f.readlines()

          for line in raw_lines:
              if line.startswith("diff --git"):
                  parts = line.strip().split(" b/")
                  if len(parts) >= 2:
                      current_file = parts[-1].strip()
                      if current_file not in files_data:
                          files_data[current_file] = []
                  new_line = 0
              elif line.startswith("@@"):
                  m = re.search(r'\+(\d+)', line)
                  if m:
                      new_line = int(m.group(1)) - 1
              elif line.startswith("+") and not line.startswith("+++"):
                  new_line += 1
                  if current_file:
                      files_data[current_file].append({
                          "line": new_line,
                          "content": line[1:].rstrip()
                      })
              elif not line.startswith("-") and not line.startswith("\\"):
                  new_line += 1

          # ----------------------------------------------------------------
          # PART C: Build enriched chunks — one per file (with safety split)
          # Each line gets:
          #   - context_before: N real lines from the actual file before it
          #   - context_after:  N real lines from the actual file after it
          #   - a numeric ID (AI uses this, never the file path)
          # ----------------------------------------------------------------
          chunks = []
          global_id_counter = 0

          for filepath, added_lines in files_data.items():
              if not added_lines:
                  continue

              # Read the full file for context
              all_file_lines = []
              if os.path.exists(filepath):
                  with open(filepath, "r", errors="ignore") as f:
                      all_file_lines = f.readlines()
              total_file_lines = len(all_file_lines)
              language = get_language(filepath)

              # Enrich each added line with context and assign ID
              enriched = []
              for entry in added_lines:
                  n = entry["line"]
                  lid = global_id_counter
                  global_id_counter += 1

                  ctx_before, ctx_after = [], []
                  if all_file_lines:
                      start = max(0, n - context_size - 1)
                      ctx_before = [
                          f"line {start+i+1}: {all_file_lines[start+i].rstrip()}"
                          for i in range(n - 1 - start)
                      ]
                      start_after = n
                      end_after = min(total_file_lines, n + context_size)
                      ctx_after = [
                          f"line {start_after+i+1}: {all_file_lines[start_after+i].rstrip()}"
                          for i in range(end_after - start_after)
                      ]

                  enriched.append({
                      "id":             lid,
                      "line":           n,
                      "content":        entry["content"],
                      "context_before": ctx_before,
                      "context_after":  ctx_after
                  })

              # Safety split: if this file has too many added lines,
              # split into parts at blank lines where possible
              if len(enriched) <= max_per_file:
                  parts_list = [enriched]
              else:
                  parts_list = []
                  current_part = []
                  for item in enriched:
                      current_part.append(item)
                      # Split at blank lines when we hit the limit
                      if len(current_part) >= max_per_file and item["content"].strip() == "":
                          parts_list.append(current_part)
                          current_part = []
                  if current_part:
                      parts_list.append(current_part)

              for part_idx, part in enumerate(parts_list):
                  part_label = f" (part {part_idx+1}/{len(parts_list)})" if len(parts_list) > 1 else ""
                  chunks.append({
                      "file":             filepath,
                      "language":         language,
                      "total_file_lines": total_file_lines,
                      "part_label":       part_label,
                      "lines":            part
                  })

          with open("diff_chunks.json", "w") as out:
              json.dump(chunks, out, indent=2)

          total_added = sum(len(c["lines"]) for c in chunks)
          print(f"Files to review: {len(files_data)}")
          print(f"Total chunks (after safety split): {len(chunks)}")
          print(f"Total added lines: {total_added}")
          for c in chunks:
              print(f"  {c['file']}{c['part_label']}: {len(c['lines'])} added lines in {c['total_file_lines']}-line file")
          EOF

      # Step 7: Call OpenAI — one API call per file chunk
      #
      # The prompt now tells the AI:
      #   - exactly which file it is reviewing
      #   - the language
      #   - total size of the file
      #   - ALL changes in that file together
      #
      # This gives the AI the same mental model a senior developer has:
      # reviewing one complete file at a time, understanding the full scope
      # of what changed and how the changes relate to each other.
      - name: Call OpenAI per file chunk and collect comments
        if: steps.check_reviewed.outputs.already_reviewed == 'false' && steps.validate_diff.outputs.too_large == 'false'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python3 << 'EOF'
          import json, urllib.request, os, time

          with open("diff_chunks.json", "r") as f:
              chunks = json.load(f)

          api_key = os.environ["OPENAI_API_KEY"]
          all_comments = []

          # Rebuild id_map from chunks so we can resolve AI IDs back to file+line
          id_map = {}
          for chunk in chunks:
              for item in chunk["lines"]:
                  id_map[item["id"]] = {"file": chunk["file"], "line": item["line"]}

          for idx, chunk in enumerate(chunks):
              filepath   = chunk["file"]
              language   = chunk["language"]
              total_lines = chunk["total_file_lines"]
              part_label  = chunk["part_label"]
              added_lines = chunk["lines"]

              print(f"\nReviewing: {filepath}{part_label} ({len(added_lines)} changes in {total_lines}-line file)")

              # Build the diff text — full file context per line
              diff_text = ""
              for item in added_lines:
                  diff_text += f"\n--- ID:{item['id']} (line {item['line']}) ---\n"
                  if item["context_before"]:
                      diff_text += "Context before:\n"
                      for c in item["context_before"]:
                          diff_text += f"  {c}\n"
                  diff_text += f">>> ADDED LINE {item['line']} (ID:{item['id']}): {item['content']}\n"
                  if item["context_after"]:
                      diff_text += "Context after:\n"
                      for c in item["context_after"]:
                          diff_text += f"  {c}\n"

              system_prompt = (
                  "You are a senior software engineer doing a thorough, file-aware code review. "
                  "You see ALL changes in a single file at once, just like a real code reviewer would. "
                  "You respond ONLY with a valid JSON array. "
                  "No markdown fences, no explanation, no preamble. "
                  "If there are no issues return exactly: []"
              )

              user_prompt = (
                  f"You are reviewing changes to: {filepath}{part_label}\n"
                  f"Language: {language}\n"
                  f"File size: {total_lines} lines total\n"
                  f"Changes in this review: {len(added_lines)} added lines\n"
                  "\n"
                  "You can see ALL the changes made to this file in this PR, along with\n"
                  "the surrounding context lines from the actual file.\n"
                  "Use this full picture to understand the intent of the changes and\n"
                  "how the changed lines relate to each other.\n"
                  "\n"
                  "Each changed line is marked with >>> and identified by ID:N.\n"
                  "Context lines (not marked with >>>) are shown for understanding only.\n"
                  "\n"
                  "FLAG a line ONLY if it has a genuine, objective issue:\n"
                  "  - Security vulnerabilities (injection, exposed secrets, broken auth,\n"
                  "    insecure deserialization, missing input validation)\n"
                  "  - Bugs that will cause crashes, data loss, or wrong results\n"
                  "    (null dereference, wrong operator, off-by-one with real impact)\n"
                  "  - Missing error handling on I/O, network calls, or DB operations\n"
                  "    that will silently fail or crash in production\n"
                  "  - Race conditions or concurrency bugs\n"
                  "  - Obvious performance issues (N+1 queries, unbounded loops on large data)\n"
                  "  - Resource leaks (unclosed files, connections, handles)\n"
                  "  - Hardcoded secrets, credentials, or environment-specific magic values\n"
                  "\n"
                  "SINCE YOU SEE THE FULL FILE CONTEXT:\n"
                  "  - If multiple changed lines form a pattern (e.g. no error handling\n"
                  "    across several DB calls), flag the first occurrence and mention\n"
                  "    the pattern — do NOT repeat the same comment on every line\n"
                  "  - If a change looks risky in isolation but is safe given the rest\n"
                  "    of the file context, do NOT flag it\n"
                  "  - Consider how changed lines interact with each other\n"
                  "\n"
                  "DO NOT FLAG:\n"
                  "  - Style, naming, or formatting preferences\n"
                  "  - Architectural opinions ('you should restructure this')\n"
                  "  - Issues clearly handled elsewhere in the visible context\n"
                  "  - Test files unless there is a definite logic bug\n"
                  "\n"
                  "OUTPUT — a JSON array. Each object must have exactly:\n"
                  '  "id"      : the integer from ID:N — just the number\n'
                  '  "comment" : one clear sentence — the specific problem and exact fix\n'
                  "\n"
                  'Example: [ {"id": 3, "comment": "..."}, {"id": 17, "comment": "..."} ]\n'
                  "If no genuine issues: []\n"
                  "\n"
                  "CHANGES TO REVIEW:\n"
                  + diff_text
              )

              payload = {
                  "model": "gpt-4o",
                  "messages": [
                      {"role": "system", "content": system_prompt},
                      {"role": "user",   "content": user_prompt}
                  ],
                  "max_tokens": 2000,
                  "temperature": 0.1
              }

              for attempt in range(3):
                  try:
                      req = urllib.request.Request(
                          "https://api.openai.com/v1/chat/completions",
                          data=json.dumps(payload).encode(),
                          headers={"Content-Type": "application/json", "Authorization": f"Bearer {api_key}"}
                      )
                      with urllib.request.urlopen(req) as response:
                          data = json.loads(response.read())

                      raw = data["choices"][0]["message"]["content"].strip()
                      if raw.startswith("```"):
                          raw = "\n".join(raw.split("\n")[1:])
                      if raw.endswith("```"):
                          raw = "\n".join(raw.split("\n")[:-1])
                      raw = raw.strip()

                      file_comments = json.loads(raw)
                      resolved = []
                      for c in file_comments:
                          lid = int(c["id"])
                          if lid in id_map:
                              resolved.append({
                                  "file":    id_map[lid]["file"],
                                  "line":    id_map[lid]["line"],
                                  "comment": c["comment"]
                              })
                          else:
                              print(f"  WARNING: unknown ID {lid}, skipping")

                      all_comments.extend(resolved)
                      print(f"  -> {len(resolved)} comments")
                      break

                  except Exception as e:
                      print(f"  Attempt {attempt+1} failed: {e}")
                      if attempt < 2:
                          wait = 2 ** attempt
                          print(f"  Retrying in {wait}s...")
                          time.sleep(wait)
                      else:
                          print(f"  Failed after 3 attempts, skipping {filepath}")

              # Small delay between files to avoid rate limiting
              if idx < len(chunks) - 1:
                  time.sleep(1)

          with open("line_comments.json", "w") as f:
              json.dump(all_comments, f, indent=2)
          print(f"\nTotal comments across all files: {len(all_comments)}")
          EOF

      # Step 8: Post comments — two-tier, zero lost
      #
      # TIER 1 — Inline: lines confirmed in GitHub's diff (valid_lines.json)
      # TIER 2 — Fallback PR comment: anything outside diff view, never lost
      - name: Post comments (inline + fallback for out-of-diff lines)
        if: steps.check_reviewed.outputs.already_reviewed == 'false' && steps.validate_diff.outputs.too_large == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
          PR_COMMIT_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          python3 << 'EOF'
          import json, urllib.request, urllib.error, os

          with open("line_comments.json", "r") as f:
              comments = json.load(f)
          with open("valid_lines.json", "r") as f:
              valid_list = json.load(f)

          valid = {(e["file"], e["line"]) for e in valid_list}

          token      = os.environ["GITHUB_TOKEN"]
          repo       = os.environ["REPO"]
          pr_number  = os.environ["PR_NUMBER"]
          commit_sha = os.environ["PR_COMMIT_SHA"]

          posted   = 0
          fallback = []

          for item in comments:
              if (item["file"], item["line"]) not in valid:
                  fallback.append(item)
                  print(f"Queued for fallback: {item['file']} line {item['line']}")
                  continue

              payload = {
                  "body":      f"\U0001f916 **AI Review:** {item['comment']}",
                  "commit_id": commit_sha,
                  "path":      item["file"],
                  "line":      item["line"],
                  "side":      "RIGHT"
              }
              req = urllib.request.Request(
                  f"https://api.github.com/repos/{repo}/pulls/{pr_number}/comments",
                  data=json.dumps(payload).encode(),
                  headers={
                      "Authorization": f"Bearer {token}",
                      "Content-Type": "application/json",
                      "Accept": "application/vnd.github.v3+json"
                  }
              )
              try:
                  with urllib.request.urlopen(req) as r:
                      if r.status == 201:
                          posted += 1
                          print(f"Inline: {item['file']} @ {item['line']}")
              except urllib.error.HTTPError as e:
                  fallback.append(item)
                  print(f"Recovered to fallback: {item['file']} line {item['line']}: {e.read().decode()}")

          if fallback:
              body_lines = [
                  "\U0001f916 **AI Review \u2014 Additional Findings**\n",
                  "The following issues could not be posted as inline comments (outside the diff view):\n"
              ]
              for item in fallback:
                  body_lines.append(f"- **`{item['file']}` line {item['line']}:** {item['comment']}")
              req = urllib.request.Request(
                  f"https://api.github.com/repos/{repo}/issues/{pr_number}/comments",
                  data=json.dumps({"body": "\n".join(body_lines)}).encode(),
                  headers={
                      "Authorization": f"Bearer {token}",
                      "Content-Type": "application/json",
                      "Accept": "application/vnd.github.v3+json"
                  }
              )
              with urllib.request.urlopen(req) as r:
                  print(f"Fallback comment posted ({len(fallback)} items): {r.status}")

          with open("posting_stats.json", "w") as f:
              json.dump({"inline": posted, "fallback": len(fallback)}, f)

          print(f"\nSummary: {posted} inline, {len(fallback)} fallback \u2014 0 lost")
          EOF

      # Step 9: Add ai-reviewed label
      - name: Add ai-reviewed label
        if: steps.check_reviewed.outputs.already_reviewed == 'false' && steps.validate_diff.outputs.too_large == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Content-Type: application/json" \
            "https://api.github.com/repos/$REPO/labels" \
            -d '{"name":"ai-reviewed","color":"7057ff","description":"AI code review completed"}' || true
          curl -s -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Content-Type: application/json" \
            "https://api.github.com/repos/$REPO/issues/$PR_NUMBER/labels" \
            -d '{"labels":["ai-reviewed"]}'
          echo "Label ai-reviewed applied"

      # Step 10: Post final summary
      - name: Post summary comment
        if: steps.check_reviewed.outputs.already_reviewed == 'false' && steps.validate_diff.outputs.too_large == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
          TOTAL_LINES: ${{ steps.validate_diff.outputs.total_lines }}
        run: |
          python3 << 'EOF'
          import json, os, urllib.request

          with open("posting_stats.json") as f:
              stats = json.load(f)
          with open("diff_chunks.json") as f:
              chunks = json.load(f)

          inline   = stats["inline"]
          fallback = stats["fallback"]
          total    = inline + fallback

          # Build per-file breakdown for the summary
          file_summary = {}
          with open("line_comments.json") as f:
              comments = json.load(f)
          for c in comments:
              file_summary[c["file"]] = file_summary.get(c["file"], 0) + 1

          files_reviewed = len({c["file"] for c in chunks})

          file_rows = ""
          for filepath, count in sorted(file_summary.items()):
              file_rows += f"| `{filepath}` | {count} |\n"
          if not file_rows:
              file_rows = "| — | 0 |\n"

          fallback_note = (
              f"\n> \U0001f4dd **{fallback} finding(s)** could not be posted inline "
              "(outside diff view) \u2014 see the **AI Review \u2014 Additional Findings** comment above."
              if fallback > 0 else ""
          )

          body = (
              "\U0001f916 **AI Code Review Complete**\n\n"
              f"| | |\n|---|---|\n"
              f"| Files reviewed | {files_reviewed} |\n"
              f"| Lines reviewed | {os.environ.get('TOTAL_LINES', '?')} |\n"
              f"| Inline comments | {inline} |\n"
              f"| Fallback comments | {fallback} |\n"
              f"| Total findings | {total} |\n"
              f"| Label | `ai-reviewed` |\n"
              "\n"
              "**Findings by file:**\n\n"
              "| File | Comments |\n|---|---|\n"
              + file_rows
              + f"{fallback_note}\n\n"
              "\u26a0\ufe0f **AI review is a one-time pass. Please fix all comments above, "
              "then request a peer review from your teammates. AI will not review this PR again.**"
          )

          token = os.environ["GITHUB_TOKEN"]
          repo = os.environ["REPO"]
          pr_number = os.environ["PR_NUMBER"]
          req = urllib.request.Request(
              f"https://api.github.com/repos/{repo}/issues/{pr_number}/comments",
              data=json.dumps({"body": body}).encode(),
              headers={"Authorization": f"Bearer {token}", "Content-Type": "application/json", "Accept": "application/vnd.github.v3+json"}
          )
          with urllib.request.urlopen(req) as r:
              print(f"Summary posted: {r.status}")
          EOF
